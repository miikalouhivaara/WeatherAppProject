<resources>
    <!-- Perustyyli sovellukselle -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Värit -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryVariant</item>
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorSurface">@color/colorSurface</item>
        <item name="colorError">@color/colorError</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>
        <item name="colorOnSecondary">@color/colorOnSecondary</item>
        <item name="colorOnSurface">@color/colorOnSurface</item>
        <item name="colorOnError">@color/colorOnError</item>

        <!-- Fontti -->
        <item name="android:fontFamily">sans-serif</item> <!-- Käytetään Androidin sisäänrakennettua sans-serif fonttia -->
    </style>

    <!-- Tekstityyli -->
    <style name="TextAppearance.App" parent="TextAppearance.MaterialComponents.Body1">
        <item name="android:fontFamily">sans-serif</item> <!-- Fontti määritellään täällä myös -->
        <item name="android:textColor">@color/colorOnBackground</item> <!-- Tekstin väri -->
    </style>
</resources>
